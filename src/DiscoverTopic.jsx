import { View, Text } from 'react-native';
import { styles } from './styles';

const DiscoverTopic = () => (
    <View style={styles.basicPage}>
        <Text>Discover news stories and newspapers.</Text>
    </View>
);

export default DiscoverTopic;
