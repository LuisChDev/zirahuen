import { View, Text } from 'react-native';
import { styles } from './styles';

const Following = () => (
    <View style={styles.basicPage}>
        <Text>These are the journalists you follow.</Text>
    </View>
);

export default Following;
